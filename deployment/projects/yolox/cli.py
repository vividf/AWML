"""YOLOX CLI extensions."""

from __future__ import annotations

import argparse


def add_args(parser: argparse.ArgumentParser) -> None:
    """Register YOLOX-specific CLI flags onto a project subparser.

    Currently no YOLOX-specific CLI flags are needed.
    """
    pass
