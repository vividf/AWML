# BatchNorm eps 參數修復說明

## 什麼是 BatchNorm eps？

`eps` (epsilon) 是 BatchNorm 層中的一個**數值穩定性參數**，用於防止除零錯誤。

### BatchNorm 的計算公式

```
# 標準化過程
mean = mean(x)                    # 計算批次均值
var = var(x)                      # 計算批次方差
x_norm = (x - mean) / sqrt(var + eps)  # 標準化
output = gamma * x_norm + beta    # 縮放和偏移
```

**關鍵點**：`eps` 被添加到方差 (`var`) 中，防止 `sqrt(var)` 為零時出現除零錯誤。

---

## 為什麼 eps 很重要？

### 1. **數值穩定性**

當 `var` 非常小時（接近 0），`sqrt(var)` 會非常接近 0，導致：
- **除零風險**：如果 `var = 0`，`sqrt(0) = 0`，會導致 `x_norm = inf` 或 `nan`
- **梯度爆炸**：反向傳播時，梯度會變得非常大

### 2. **混合精度訓練的影響**

在 **FP16 (float16)** 混合精度訓練中：
- FP16 的數值範圍比 FP32 小得多
- **精度損失**：FP16 只能表示約 65,000 個不同的數值
- **更容易溢出**：當 `var` 很小時，`1/sqrt(var + eps)` 可能超出 FP16 範圍

### 3. **eps 值大小的影響**

#### eps = 1e-3 (0.001) - **太大，有問題** ❌

```
假設 var = 0.0001 (很小的方差)
sqrt(var + eps) = sqrt(0.0001 + 0.001) = sqrt(0.0011) ≈ 0.033

問題：
1. eps (0.001) 比 var (0.0001) 大 10 倍！
2. eps 主導了標準化過程，導致標準化不準確
3. 當 var 很小時，標準化效果被 eps 稀釋
4. 在 FP16 中，這種不準確會被放大
```

**實際影響**：
- 標準化不準確 → 特徵分佈異常
- 梯度計算異常 → `grad_norm` 可能變成 `inf` 或 `nan`
- Loss 計算異常 → 最終導致訓練崩潰

#### eps = 1e-5 (0.00001) - **標準值，穩定** ✅

```
假設 var = 0.0001 (很小的方差)
sqrt(var + eps) = sqrt(0.0001 + 0.00001) = sqrt(0.00011) ≈ 0.0105

優點：
1. eps (0.00001) 遠小於 var (0.0001)
2. eps 只在 var 極小時才起作用（防止除零）
3. 標準化過程主要由 var 決定，更準確
4. 在 FP16 中更穩定
```

**實際影響**：
- 標準化準確 → 特徵分佈正常
- 梯度計算穩定 → `grad_norm` 在合理範圍
- Loss 計算穩定 → 訓練過程穩定

---

## 為什麼從 1e-3 改為 1e-5？

### 1. **行業標準**

- **PyTorch 默認**：`eps=1e-5`
- **TensorFlow 默認**：`eps=1e-3`（但通常用於 FP32）
- **MMDetection/MMDetection3D**：推薦 `eps=1e-5`

### 2. **混合精度訓練的最佳實踐**

在 FP16 訓練中，**更小的 eps** 更合適：
- 減少數值誤差累積
- 避免標準化過程被 eps 主導
- 提高訓練穩定性

### 3. **與 grad_norm nan 的關係**

**問題鏈**：
```
eps 太大 (1e-3)
  ↓
標準化不準確
  ↓
特徵值異常（可能出現 inf/nan）
  ↓
前向傳播異常
  ↓
梯度計算異常（grad_norm = nan）
  ↓
參數更新異常
  ↓
模型崩潰（loss = nan）
```

**修復後**：
```
eps 標準 (1e-5)
  ↓
標準化準確
  ↓
特徵值正常
  ↓
前向傳播穩定
  ↓
梯度計算穩定（grad_norm 正常）
  ↓
參數更新正常
  ↓
訓練穩定
```

---

## 具體修復的三個位置

### 1. `pts_voxel_encoder` (PillarFeatureNet)
```python
norm_cfg=dict(type="BN1d", eps=1e-5, momentum=0.01)
```
- **作用**：處理點雲 voxel 特徵的標準化
- **影響**：如果這裡不穩定，會影響整個特徵提取流程

### 2. `pts_backbone` (BEVResNet)
```python
norm_cfg=dict(type="BN", eps=1e-5, momentum=0.01)
```
- **作用**：ResNet34 主幹網絡的標準化
- **影響**：這是**最關鍵的位置**，因為：
  - ResNet 層數深（34 層）
  - 沒有預訓練權重（從頭訓練）
  - 數值誤差會累積傳播

### 3. `pts_neck` (SECONDFPN)
```python
norm_cfg=dict(type="BN", eps=1e-5, momentum=0.01)
```
- **作用**：特徵金字塔網絡的標準化
- **影響**：影響多尺度特徵融合的穩定性

---

## 預期效果

### 修復前（eps=1e-3）
- ❌ `grad_norm` 可能出現 `nan` 或 `inf`
- ❌ `loss` 可能出現 `nan`
- ❌ `loss_scaler` 持續下降（從 256 → 0）
- ❌ 訓練過程不穩定

### 修復後（eps=1e-5）
- ✅ `grad_norm` 穩定在合理範圍（通常 < 20）
- ✅ `loss` 正常下降
- ✅ `loss_scaler` 穩定在初始值附近（256 左右）
- ✅ 訓練過程穩定

---

## 數值示例

### 示例 1：正常情況
```
var = 0.01 (正常方差)

eps = 1e-3:
  sqrt(0.01 + 0.001) = sqrt(0.011) ≈ 0.105
  eps 貢獻：0.001 / 0.011 ≈ 9%  ← eps 影響較大

eps = 1e-5:
  sqrt(0.01 + 0.00001) = sqrt(0.01001) ≈ 0.100
  eps 貢獻：0.00001 / 0.01001 ≈ 0.1%  ← eps 影響很小
```

### 示例 2：方差很小（危險情況）
```
var = 0.0001 (很小的方差，可能導致不穩定)

eps = 1e-3:
  sqrt(0.0001 + 0.001) = sqrt(0.0011) ≈ 0.033
  eps 貢獻：0.001 / 0.0011 ≈ 91%  ← eps 幾乎主導！
  → 標準化不準確，特徵異常

eps = 1e-5:
  sqrt(0.0001 + 0.00001) = sqrt(0.00011) ≈ 0.0105
  eps 貢獻：0.00001 / 0.00011 ≈ 9%  ← eps 只起保護作用
  → 標準化相對準確，特徵正常
```

---

## 總結

**BatchNorm eps 修復的核心價值**：

1. **防止數值溢出**：在 FP16 訓練中避免除零和溢出
2. **提高標準化準確性**：讓標準化過程主要由方差決定，而不是 eps
3. **穩定梯度計算**：減少異常梯度，防止 `grad_norm` 變成 `nan`
4. **符合最佳實踐**：使用行業標準值（1e-5）而不是過大的值（1e-3）

這是一個**關鍵的數值穩定性修復**，特別是在混合精度訓練中，它直接影響訓練的穩定性和收斂性。
